# Convex (printed during `npx convex dev`)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# OpenRouter API key — get one at https://openrouter.ai/keys
# This is set in the PromptPipe Settings UI, NOT here.
# The app encrypts and stores it in Convex (AES-256-GCM).
# OPENROUTER_API_KEY is only needed if you want to use the
# pipeline suggest feature server-side.
